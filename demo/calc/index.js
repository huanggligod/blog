(function(){
   "use strict";
   
    // 获取元素
    var el = function(element) {
        if (element.chartAt(0) === '#') {
            return document.querySelector(element)
        }

        return document.querySelector
    }

    
}())